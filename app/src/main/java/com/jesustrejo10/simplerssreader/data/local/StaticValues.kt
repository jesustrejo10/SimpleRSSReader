package com.jesustrejo10.simplerssreader.data.local

/**
 * There we should use a DataBase to store all user information but this application doesn't have any session support.
 * Also doens't have offline support so the information will get store in companion object.
 */
class StaticValues {

    companion object{
        var AUTHENTICATION = ""
    }

}